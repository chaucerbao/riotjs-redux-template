{
  "name": "riotjs-redux-template",
  "version": "0.0.1",
  "description": "A project template for building a web application with RiotJS and Redux",
  "main": "index.js",
  "scripts": {
    "start": "npm run init && node_modules/.bin/webpack-dev-server --hot -d",
    "build": "npm run init && node_modules/.bin/webpack -p --optimize-minimize --optimize-occurence-order --optimize-dedupe && gzip -k9 ./public/assets/*",
    "clean": "rm -rf ./public",
    "init": "npm run clean && mkdir -p ./public && cp ./src/index.html ./public/"
  },
  "author": "Alvin Chan <no@email.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "*",
    "babel-loader": "*",
    "babel-preset-es2015": "*",
    "css-loader": "*",
    "extract-text-webpack-plugin": "*",
    "isomorphic-fetch": "*",
    "normalize.css": "*",
    "postcss-import": "*",
    "postcss-loader": "*",
    "postcss-mixins": "*",
    "postcss-nested": "*",
    "postcss-simple-vars": "*",
    "redux": "*",
    "redux-thunk": "*",
    "riot": "*",
    "riotjs-loader": "*",
    "style-loader": "*",
    "webpack": "*",
    "webpack-dev-server": "*"
  }
}
